Linux go-plugin 构建示例

1. 进入目录: plugins/sample-goplugin/backend/go-plugin
2. 执行: bash build-linux.sh
3. 生成文件: plugins/sample-goplugin/backend/dist/plugin.so

注意:
- go-plugin 通道仅 Linux 支持。
- Windows 下该通道会返回不支持错误。
